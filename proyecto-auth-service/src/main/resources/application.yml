spring:
  application:
    name: servicio-auth

  datasource:
    url: jdbc:mysql://localhost:3306/usuario_db?createDatabaseIfNotExist=true
    username: root
    password: mysql
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8083

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true


jwt:
  secret: Ni2vMl6cc6Zop38ghg73vEp5rorkz4RhXQIqTiJQOvY
  expiration: 3600000 

logging:
  level:
    com.cibertec.auth: DEBUG
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG
    com.cibertec.auth.feign: DEBUG
    feign: DEBUG